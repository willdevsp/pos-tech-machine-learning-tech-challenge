# api

Esta pasta expõe a API do projeto.

Arquivos principais:
- `main.py`: ponto de entrada da API e definição dos endpoints.
- `schemas.py`: definições de esquemas e modelos de dados usados pela API.

A API é responsável por receber requisições de inferência, validar entradas e retornar previsões.
