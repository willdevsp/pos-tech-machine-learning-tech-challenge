services:
  api:
    build:
      context: ..
      dockerfile: api/Dockerfile
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - MLFLOW_TRACKING_URI=http://mlflow-server:5000
      - APP_PORT=8000
      - APP_HOST=0.0.0.0
    restart: always
