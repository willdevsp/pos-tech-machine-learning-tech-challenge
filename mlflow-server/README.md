# RocketTail MLflow Server Component

Este componente gerencia a inicialização local do servidor de tracking e registro de modelos do **MLflow**.

## Setup e Execução
Inicie o container do MLflow:
```bash
cd mlflow-server
docker compose up -d
```

A interface web estará disponível em `http://localhost:5000`.
